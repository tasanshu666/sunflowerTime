# 向日葵专注 SunFocus · 项目进度跟踪表

> **用途**：单一进度事实源。新会话开场读本文件即可知道「做到哪了、下一步做什么」。
> **生成/更新**：2026-09-25 **20:48** ｜ 分支 `main` ｜ 基线提交 **`58ce640`** ｜ 工作区**有未提交改动**（本轮动效+风格+4 份文档，待玄参验收后提交）
> **维护约定**：本表只记**事实**（带 `文件:行号` 或命令实证）。方向性建议统一放 `docs/交接总结_下一阶段.md` §6，**不在此重复**，避免两处真相。
> **优先级口径**：P0 = 不做就发不出去 / 功能承诺缺失；P1 = 上架必需材料；P2 = 体验与整洁度。

---

## 0. 一屏仪表盘

| 维度 | 状态 | 进度 |
|---|---|---|
| 功能里程碑 M0–M3 | ✅ 主体完成 | `██████████████████░░` 约 90%（本轮补齐植物养成规则闭环 + 花谢循环 + 花园美术接入） |
| 功能里程碑 M4（打磨上架） | ⚠️ 约 4/10 | `████████░░░░░░░░░░░░` 约 40%（**P0 功能缺口已清零**；剩上架材料：隐私政策 / 商店文案 / 截图 ⬜） |
| 上架硬阻塞 | ✅ **0 条**（release 签名已接入自有密钥 + 包名已解决） | `████████████████████` 100% |
| 上架材料（P1） | 🟡 **2 / 3**（隐私政策 ✅ / 商店文案 ✅ 已出文档；商店截图清单已出、待玄参真机自截） | `█████████████░░░░░░` ~67% |
| 美术接入链路 | ✅ 贯通 | `████████████████████` 100%（已实机验证「换图→界面真变」） |
| 美术资源本身 | ⚠️ 首物种已交付 | `██████░░░░░░░░░░░░░░` **7 / 24**（向日葵生效张数 / 重算后总槽位）+ 通用盆 1 + 背景 1 |
| 养护动效（浇水/施肥/开花） | 🟡 **代码动效已上（浇水/施肥）**，序列帧接口预留；开花/成长过渡待序列帧（见新方案） | `███████░░░░░░░░░░░` ~40% |
| 真机验证 | ⚠️ **待小米 14 Pro 复验** | 上次**真机**装机 2026-09-23 21:41；**之后经过 G17–G21 五轮修复 + 施肥调参，均未上真机**；模拟器（iPhone 17）已验到 2026-09-25 |

| 关键数字 | 值 | 出处 |
|---|---|---|
| `main` 提交数 | **31** | `git rev-list --count main` |
| `flutter analyze` | **0 error**（86 issues 全为既有 info/warning 存量） | 本轮实测 |
| `flutter test` | **457 条全绿**（本轮 +4 动效用例） | 2026-09-25 晚严过关实测（`env -u HTTP_PROXY … flutter test --no-pub`） |
| 数据库 schemaVersion | **8**（v8 新增 `plants.bloomed_at`） | `app_database.dart` |
| 未提交改动 | **有**（动效+风格+4 文档，待玄参验收） | `git status --short` |

---

## 1. ✅ 已完成

### 1.1 里程碑（M0–M3）

| 里程碑 | 范围 | 状态 | 证据 |
|---|---|---|---|
| **M0 骨架** | 工程 / 导航 / 加密本地库（Drift+SQLCipher）/ PIN 锁 / 同意流 | ✅ 完成 | `docs/软件设计文档_M0.md` |
| **M1 专注闭环** | 打盹屏 / 四档反馈 / 在场检测 / 阳光产出 / 结算动画 / WFD | ✅ 完成 | `docs/软件设计文档_M1.md` |
| **M2 经济与商店核销** | 奖励表 / 兑换队列 / 家长核销 / 免确认 / 周池 / 冷却期 | ✅ 完成 | `docs/软件设计文档_M2.md` |
| **M3 花园·任务·家长端** | 植物养成（3 物种 × 3 阶段）/ 任务打卡 / 家长端 4 页 / 删除入口 | ✅ 完成（**缺纪念册埋点**，见 §3.2） | `lib/data/local/plant_seed.dart:20-48` |

### 1.2 已完成的功能/修复（按时间倒序）

| # | 项 | 提交 | 说明 |
|---|---|---|---|
| 1 | **植物状态规则重构 + 花谢循环 + 美术接入 + 花园页 v2/v3 + P0 缺口补全** | **`4535353`**（2026-09-25 已提交推送） | 含 §1.3 全部子项 |
| 2 | 包名统一（Android/iOS） | `9e6da42` | `com.example.sunflower_time`（下划线）/ `com.example.sunflowerTime` → **`com.sunflowertime.app`**（F31 结案）；不做数据迁移（玄参拍板） |
| 3 | M3/M4 文档补齐 + 进度表 + 交接总结 | `8302837` | 新增 3 份文档 |
| 4 | **美术接入链路修复** | `2725f25` | `AssetManifest.json` 读取失效（Flutter 3.7+ 只产 `.bin`）→ 改 `loadFromAssetBundle` + `listAssets()`；契约抽成公开 `PlantArtCandidates` |
| 5 | **日上限口径重构** | `a3c31c0` | 取消分段软顶 → 两条独立额度线（专注 60/90/120；成长奖励独立 79）；额度三道拦；打卡与专注额度解耦。见宪法 C11 |
| 6 | 花园显示改造 | `c0f8298` | 卡片列表 → 草地 + 花盆网格；点植物弹养护面板 |
| 7 | 专注上限三档阶梯 | `69e9209` | 周池上限收敛 500；删除「核销履约率」指标 |
| 8 | 防作弊（M4 经济侧） | `421df23` | `CheckInStatus.{verified,pending,rejected}` 把 index 0 留给 `verified`；`pending` 不占当日软顶；核销防重复入账 |

### 1.3 大批次（原「未提交」，**2026-09-25 已提交推送**：`4535353` 代码+测试、`58ce640` 文档）

| 子项 | 内容 | 口径出处 |
|---|---|---|
| **植物状态规则** | 枯萎 7 → **3 天**；**取消付费救回**（`revive()` 与 `kPlantReviveCost*` 整段删除）→ 改**养护恢复**（未满 3 天浇水 1 次 / 满 3 天浇水 3 次 + 施肥 1 次）；恢复次数**以账本为唯一事实源**（`countByRefTypeAndRefIdSince`，不给 Plants 表加列） | 宪法 **C12** |
| **花谢循环** | 盛开保持 `kBloomDurationDays = 3` 天 → 花谢（`status → growing`、进度回落 `kBloomWiltProgressFloor = 0.5`），**体型保持成株**；养满自动再盛开。`Plant.bloomedAt`（v8 迁移） | 宪法 C12 |
| **美术接入（首批）** | 向日葵 **10 张** + 通用 `pot.png` + 背景 `background.png`；pubspec 新增 `assets/pots/`、`assets/garden/`；背景铺满 body；三种格子统一 `pot.png` | 宪法 **C13** |
| **美术根因修复** | ① 原图 2048×2048 但内容仅占 35%×30% → 统一归一化为 **1200×2000**（盆宽 800、盆底贴底、盆心 x=600），新增 `tools/normalize_plant_art.py`（幂等，原图备份 `assets/_originals/` 不入包）；② 植物图**自带花盆**而代码又叠一层 → **代码不再叠盆**，`GardenPot` 只渲染一张植物图；③ `growthScale` 0.18 → **0**（所有盆必须等大） | 宪法 C13 |
| **花园页 v2** | 「我的阳光」通栏卡 → **顶部栏左上角胶囊**（新建 `sunlight_pill.dart` + `sunlightBalanceProvider`，仅花园 tab）；空花盆只显示陶盆（去掉「+ 点击种植」） | — |
| **花园页 v3** | 网格**锁 2 行**（超出**区域内纵向滚动**，滚动条仅溢出时可见）；加号格圆**对齐盆心**（`pot.png` 盆心在画布 82.95%）；三类格子文案统一**白胶囊底**（枯萎态对比度 3.50 → 5.18）；底部白块**整块删除**；背景图左下角**木牌可点击**（呼吸高亮）→ 弹**「玩法说明」**（玄参拍板：不再叫「花园说明」） | 宪法 C13 |
| **P0 缺口补全** | ① **调试代码清理**：`child_profile_page.dart` 的 `DEBUG 加1000阳光` + `app_router.dart` `/s1-demo` 路由 + `s1_demo_page.dart` 文件（grep 证实 `lib/` 内 0 引用）；② **App 总时长防沉迷**：新增 `lib/domain/services/app_usage_service.dart`（`advance` + `isCapReached` **唯一判定入口**）+ `lib/presentation/child/state/app_usage_controller.dart`；③ **纪念册埋点**：新增 `lib/domain/services/memoir_service.dart` + 事件名常量 | 玄参 2026-09-23 拍板 3 条口径（计时只算花园/商店/我的；`praise_sent` 家长写就记；里程碑 4 类） |
| **种子通用契约** | `plant_artwork.dart` 候选 **3 级 → 5 级**：`{id}_{stage}_{status}` → `{id}_{stage}` → `{id}` → `shared_{stage}_{status}` → `shared_{stage}`（**物种图恒优先于通用图**）；命名契约抽成公开纯类 `PlantArtCandidates`，护栏测试与实现**共用同一份** | 玄参拍板「种子通用，幼苗/成株按物种」 |
| **背景换图适配** | `background.png` 换新图 **1056×2336**（原 1161×2560）→ `kGardenBackgroundSize` 重设、木牌热区重新标定为 `Rect.fromLTRB(0.082, 0.642, 0.258, 0.722)`；新增背景图资产守卫测试（直读 PNG IHDR 比对常量） | 宪法 C13 |
| **真机 Bug ① 阳光溢出** | 左上角阳光余额位数一多（≥4 位）→ AppBar `leading` 放不下 → **数字竖排溢出**。修：`sunlight_pill.dart` 内容包 `FittedBox(BoxFit.scaleDown)` + `leadingWidth` 96 → **112**；新增 `test/m3/sunlight_pill_test.dart`（6 位余额窄约束不溢出） | 玄参 2026-09-23 真机反馈（附图） |
| **真机 Bug ② 加号点不动** | 花园页 v3 重写**弄丢了 `economyRevisionProvider` 监听** → 别处扣款后 `_balance` 仍是旧值 → `shortfall` 误算为正 → 判「阳光不足」→ `onTap` 传 null。修：`GardenPage.build()` 内补 `ref.listen(economyRevisionProvider, …)` 静默重载 | 同上（**回归**，影响真实流程） |
| **dispose 停表泄漏（测试抓出）** | 外壳 `dispose()` 里用 `ref` 读 provider 会抛 `Bad state: Cannot use "ref" after the widget was disposed.`（Riverpod 的 `ConsumerStatefulElement.unmount` 先标 disposed 再调 `state.dispose`）→ 被 `catch (_)` **静默吞掉** → ticker 从未取消 → **真机定时器泄漏**（进锁屏/entry 页后娱乐时长仍在偷偷累加）。修：`initState` 抓 notifier 引用存字段，dispose **不走 ref** | 新增 dispose 停表用例（修复前红、修复后绿） |
| **模拟器轮 G17/G18（2026-09-24）** | 玄参 iPhone 17 模拟器验收反馈两条：① 点「+」**仍无响应** → `ExpandPotSlot` 阳光不足时 `onTap: null` 静默拦截（旧测试还把「点了没反应」锁成预期）→ 改为**仍回调**、由页面层弹分因 SnackBar；② 养护面板**黄黑溢出条贯穿全屏** → `PlantArtwork` 的 `Image.asset` 无显式宽高，命中美术图后按原图逻辑尺寸（400×667pt）撑爆 loose 约束的 Row（探针实测 overflow 870px）→ 图片包 `SizedBox(size)`。**验证靠临时探针入口**（widget 测试因 AssetManifest 走占位而假绿）；探针复跑 0 overflow。详见 `bug记录表.md` G17/G18 | 玄参 2026-09-24 模拟器反馈（附截图） |
| **赠予 UX + 调试入口加回（2026-09-24 深夜）** | ① 赠予 30 阳光「孩子端没收到」→ 根因是**日上限 20**（PRD §4.5）把赠予挡了、旧实现只弹一闪而过的 SnackBar（**静默失败**），同步链路本身没问题 → 弹窗改为**显示已赠/剩余额度 + 超额实时报错并禁用「赠予」按钮**；② 玄参要求把「DEBUG 加1000阳光」**加回** → 升级为受 `kDebugMode` 约束（**release 自动隐藏**，不必再担心提审前忘了删），且加完后**自增经济修订号**（旧版只刷本页）。测试：「不得出现 DEBUG」改为**源码守卫**（正则断言 `if (kDebugMode) …`）+ 新增「点按钮真的入账 1000」 | 玄参 2026-09-24 反馈 |
| **施肥进度调参（2026-09-25）** | 玄参拍板「施肥增长 5% 的进度有点快」→ `kPlantFertilizeProgressGain` **0.05 → 0.03**（`prd_params.dart:203-206`，唯一真源，UI 文案从常量派生故无需改代码）；每天满养护推进 **18% → 16%**；满养护长成 **17 天 → 约 19 天**（实测 19 天 = 理论 18.75 进位）；**不养护 30 天不变**。同步：`test/m3/plant_growth_v2_test.dart` 常量钉死断言改 `0.03` / 养护上限 `0.06` / 长成区间 `19..20`；`美术资源清单_花园植物.md`、`软件设计文档_M3M4.md`、`产品开发文档_M2M3M4.md`、`动画视频提示词_豆包_v1.md` 施肥表述全部 5% → 3%；`bug记录表.md` 新增 **F40**；`口径裁定表_v1.md` 变更记录追加 2026-09-25 | 玄参 2026-09-25 拍板 |
| **养护面板美化 G21（2026-09-25）** | 玄参模拟器反馈「卡片信息看不全、不美观、不要通底白」→ `plant_card.dart` 由通底白 `Card` 改为**白色圆角大卡 + 四层分区**（头部 / 成长分区 / 心情徽章 / 动作区），心情按 happy/calm/thirsty 三色着色；浇水施肥改**双行式按钮**（上行「图标+动作+增幅」、下行 `$cost ☀` 独立成行）→ 修掉窄屏把**价格**截断成「浇水 +1% · 5...」的问题；`plant_care_sheet.dart` 面板底色改暖奶油 `0xFFFBF4E4`，与白卡形成两层。新增 `test/m3/plant_care_sheet_iphone_test.dart`（iPhone 17 402×874 真实渲染 + 点花盆弹面板 + 无异常）。**玄参已验收：「改的挺好」** | 玄参 2026-09-25 模拟器反馈（附截图） |

### 1.4 分支与仓库

| 项 | 值 |
|---|---|
| 分支 | `main`（`HEAD` = `origin/main` = **`58ce640`**，2026-09-25 已推送 GitHub） |
| 开发分支 | `m0/skeleton` / `m1/focus-loop` / `m2/economy` / `spike/s1-s2-s3` —— **全部领先 main 0 个提交**，无搁浅工作 |
| ⚠️ 纪律张力 | `main` 上存在 `421df23 Merge branch 'm2/economy' into main`（历史既成事实）。**后续新分支仍按玄参纪律「不合回 main」执行**，否则模拟器调试会失效 |

---

## 2. 🔄 正在进行 / 悬空未收口（2026-09-25 19:20 更新）

> ✅ **提交已收口**：2026-09-25 玄参授权提交 → 代码+测试 `4535353`、文档 `58ce640` 均已推送 GitHub，工作区干净（0 未提交）。
> ⚠️ 当前真正悬空四项：**① 小米 14 Pro 真机复验**　**② 上架材料 0/3**　**③ 美术 17 张待出**　**④ 养护动效未开始**。

### 2-1. ✅ 已收口：大批次（2026-09-25 提交 `4535353` / `58ce640`）

| 项 | 内容 |
|---|---|
| 范围 | 植物状态规则（C12）+ 花谢循环 + 美术接入与根因修复 + 花园页 v2/v3（C13）+ P0 缺口补全（调试代码清理 / App 总时长防沉迷 / 纪念册埋点）+ 种子通用契约 + 背景换图适配 + **G17–G21 五轮真机/模拟器 Bug 修复** + 施肥调参 |
| 改动 | **41 个文件（代码+测试+背景图）+ 11 份文档** |
| 验证状态 | ✅ `flutter analyze` **0 error**；✅ 全量测试 **453 全绿**；✅ iPhone 17 模拟器已装机验收（G21 玄参口头确认「改的挺好」）；⚠️ **小米 14 Pro 真机未复验** |
| 交付物 | `assets/plants/` 现有 **10 张**向日葵图（新契约下 **7 张生效**） |
| 下一步 | ✅ 已完成（提交并推送） |

**已真机验证的三点（`a3c31c0` 日上限）**：① 选时长页超额度档位置灰 ② 结算硬截断 ③ 专注拿满后打卡**仍能**拿到成长奖励。

### 2-2. ⚠️ 当前悬空（按阻塞程度排序）

| # | 悬空项 | 状态 | 为什么悬着 |
|---|---|---|---|
| 1 | **小米 14 Pro 真机复验** | ⬜ 未做 | G17–G21 五轮修复 + 施肥调参**只上过 iOS 模拟器**（玄参人在外面，选了「先不装，稍后自己安排」）。真机与模拟器差异点：刘海屏/手势区、真实字体渲染、Android 返回键 |
| 2 | **上架材料 3 项** | 🟡 2/3 | 隐私政策 ✅ `docs/隐私政策_向日葵专注_v1.md` / 商店文案 ✅ `docs/商店文案_国内安卓_v1.md`；商店截图清单已出，待玄参真机自截。托管 URL/资质待补（§3.3 #6） |
| 3 | **美术 17 张** | ⬜ 0 / 17 | 通用种子 2 + 雏菊 7 + 仙人掌 8；链路已贯通，出图即生效 |
| 4 | **养护动效 + 界面美化** | 🟡 进度中 | 浇水/施肥代码动效 ✅ 已上（预留序列帧）；成长/商店页风格改造 ✅ 已上；**2026-09-26 新增**：分类功能（真DB字段+固定枚举）、家长端奖励/成长奶油卡美化、400周池简化、成长页卡片两段式修复、商店卡图标放大+限次收行内 🟢 已上（均 463 测试绿）；**玄参真机预览后成长/商店卡片仍不满意 → 继续迭代中**；开花/成长过渡序列帧 + 风格铺开待办（决策点 E/G 已定，见 §5） |

---

## 3. ⛔ 待完成

### 3.1 P0 —— 上架硬阻塞

| # | 项 | 现状 | 修法与连带影响 | 需决策 |
|---|---|---|---|---|
| 1 | ✅ **~~release 用 debug 签名~~**（已解决） | **2026-09-23（提交 `669041a`）接入自有发布密钥**：`android/app/upload-keystore.jks`（别名 `upload`，SHA-256 `9949e17e…8e3d4883`）；原 `signingConfig = signingConfigs.getByName("debug")` 已替换为自签配置。详见 `docs/签名密钥与发布.md` | ⚠️ 切换签名后 `adb install -r` **必失败**（`INSTALL_FAILED_UPDATE_INCOMPATIBLE`，需先卸载，而**卸载会清空 App 本地数据**：植物 / 阳光 / 专注记录） | ✅ 已定 |
| 2 | ~~`applicationId` 是 example 占位~~ | ✅ **已解决** `9e6da42`：`com.sunflowertime.app`（Android/iOS 统一） | — | ✅ 已定 |
| 3 | ✅ **~~调试代码未清~~**（**2026-09-23 晚 已清**） | ① `child_profile_page.dart` 的 `DEBUG 加1000阳光` 段与「调试区」标题已删；② `app_router.dart:73-76` 的 `/s1-demo` 路由 + import 已删，`s1_demo_page.dart` 文件已删（grep 证实 `lib/` 内 0 引用）；③ 新增回归用例「孩子端『我的』页不得再出现 DEBUG 入口」 | 无（`SunflowerCanvas`/`FeedbackOverlay` 仍被 `focus_page`/`settle_page` 使用，不受影响） | ✅ 已结案 |

### 3.2 P0 —— 已承诺但未实现的功能缺口

| # | 项 | 现状 | 影响 | 出处 |
|---|---|---|---|---|
| 4 | ✅ **~~App 总时长 ≤30 分钟 防沉迷~~**（**已实现**） | 新增 `lib/domain/services/app_usage_service.dart`（`advance` + `isCapReached` **唯一判定入口**）+ `lib/presentation/child/state/app_usage_controller.dart`（`Timer.periodic` 计时、切后台暂停、跨自然日清零）；`anti_addiction_service.isAppCapReached` 改为**一行委托**（判定孪生已消除）；外壳拦**花园/商店/我的**三个娱乐 tab，**今日/成长永远放行** | — | ✅ 已结案 |
| 5 | ✅ **~~纪念册埋点~~**（**已实现**） | 新增 `lib/domain/services/memoir_service.dart`（`ensureWeeklySnapshot` / `recordMilestone` / `recordPraiseSent`）+ 事件名常量；口径按玄参 2026-09-23 拍板：① 计时只算**花园/商店/我的**；② `praise_sent` 以「家长写就记」为 emit 点；③ 里程碑 4 类 | — | ✅ 已结案 |

### 3.3 P1 —— 上架材料与质量

| # | 项 | 状态 |
|---|---|---|
| 6 | 隐私政策 | 🟢 **已出文档** `docs/隐私政策_向日葵专注_v1.md`（国内安卓口径；核实 manifest 无网络权限，零上传主张自洽）。⚠️ 待补：公网托管 URL / 开发者实体资质（占位）/ `ACCESS_NOTIFICATION_POLICY` 是否实装待定（见 §6 F 新增） |
| 7 | 商店文案 | 🟢 **已出文档** `docs/商店文案_国内安卓_v1.md`（名称候选 / 双版描述 / 关键词 / 分级 / 审核自检） |
| 8 | 商店截图 | 🟡 清单已出 `docs/商店截图清单.md`（5 屏摆拍前置+尺寸+命名）；**由玄参真机自截**（AI 不截图） |
| 9 | 崩溃率 / 性能度量 | ⬜ 无上报机制，未度量 |
| 10 | 文案终审 | ⬜ 未做 |
| 11 | **重建「当前真实完成度」验收清单** | ⬜ `docs/验证计划_SunFocus_G0G2.md` 的 35 项 DoD **只有 1 项被勾**，其余 34 项仍是 `- [ ]`，而 M0–M3 功能**已实现** → **该清单早已不维护，不能用它反推完成度** |

### 3.4 P2 —— 体验与整洁度（不阻塞）

| # | 项 | 出处 |
|---|---|---|
| 12 | 注释里「任务」字样约 **26 处**未统一为「成长」（非用户可见，但误导新维护者） | F35 |
| 13 | `bloomed` 实际**不可**浇水/施肥，但 `enums.dart` 注释写「仍可被浇水」→ 注释与代码矛盾 | F13 |
| 14 | 过时注释仍写「7 天枯萎 / 20 阳光救回」（已取消付费救回） | `enums.dart:40/65`、`providers.dart:150` |
| 15 | PRD 与代码多处不一致（8 物种 vs 3、4 阶段 vs 3、浇水价 20 vs 5、花盆「展示权益」无分级等） | `docs/美术资源清单_花园植物.md` §9 |
| 16 | `PlantMood` 只影响 emoji 文案，不影响任何绘制 | `plant_card.dart:82-91` |
| 17 | ~~满养护 17 天 vs 测试写 18 天~~ → **2026-09-25 施肥调参后已变为 19 天（实测）**，精品 28 天；F33「就 17 天，不细调」的拍板**已被本次调参覆盖** | F33 → **F40** |

### 3.5 遗留验收（M1）

| 项 | 状态 | 出处 |
|---|---|---|
| 真机 11 步验收清单签字 | ⬜ 未签字 | `docs/人工清单.md:23-38` |
| 「两周后交给白术三样」回执 | ⬜ 无回执 | `docs/人工清单.md:39-42` |

---

## 4. 🎨 美术线（独立可并行）

| 阶段 | 项 | 状态 | 说明 |
|---|---|---|---|
| 前置 | ArtManifest 读取链路 | ✅ **已修通** | `2725f25` |
| 清单 | 出图清单已产出 | ✅ | `docs/美术资源清单_花园植物.md`（**v2.1，2026-09-23「种子通用」重算**）：**24 张** = 通用种子 2 + 向日葵 7 + 雏菊 7 + 仙人掌 8（含 `seed_dead`） |
| 决策 | 花盆怎么出图 | ✅ **已定** | 宪法 **C13**：花盆**走上 PNG 美术**（`assets/pots/pot.png`），且**植物图自带盆、代码不叠** |
| 规格 | 统一画布规格 | ✅ **已定并落地** | 宪法 **C13**：**1200×2000**（盆宽 800、盆底贴底、盆心 x=600）；归一化脚本 `tools/normalize_plant_art.py` |
| 产出 | 向日葵 | ✅ **10 张已接入**（新契约下 **7 张生效**） | `assets/plants/species_sunflower_*`（含 seed/sprout/adult × growing/wilting/dead + adult_bloomed）；种子 3 张改由通用图承担 |
| 产出 | 通用种子 | ⬜ **0 / 2** | `shared_seed.png` / `shared_seed_wilting.png`（玄参 2026-09-23 拍板「种子通用」） |
| 产出 | 小雏菊 / 仙人掌 | ⬜ **0 / 15**（雏菊 7 + 仙人掌 8） | 命名即接口，丢进 `assets/plants/` + **跑归一化脚本**即生效。⚠️ 仙人掌需**多出 1 张 `seed_dead`**（精品阶段长，枯萎死亡发生在种子期）；两物种的 `seed`/`seed_wilting` **不必出**（走通用图） |
| 产出 | 通用陶盆 / 草地背景 | ✅ **已交付并接入** | `assets/pots/pot.png`、`assets/garden/background.png` |
| 产出 | 徽章（空盆/加盆） | ⏸ 保留矢量图标 | `Icons.add` / `Icons.add_circle_outline`，本期不出图 |
| **动效** | **养护动效（浇水 / 施肥 / 开花）** | 🟡 **代码动效已实现** | 玄参 2026-09-25 提出。路线已定（决策点 **E**：**先代码后序列帧**）→ 本轮落地**浇水/施肥纯代码动效**（`care_effect_overlay.dart`：水滴下落/施肥闪光/植物弹跳，有限时长 1400ms；`garden_page` 按花盆 GlobalKey 定位叠加；预留 `frames` 序列帧接口，默认 null 走代码粒子）。**开花动画 + 成长过渡（种子→幼苗→成株）待序列帧**，制作指南见 `docs/植物成长动画方案_种子到成株_v1.md`。参考 `docs/动画视频提示词_豆包_v1.md` |
| 后期 | 打盹屏大向日葵（动态） | ⏸ 本期不做 | 与花园向日葵是**两套独立形象**；提示词清单见 `docs/动画视频提示词_豆包_v1.md` |

### 美术命名现状（与出图契约的对应关系）

玄参本轮实际采用的是**「基础态用二级、特殊态用一级」**的混合命名，**合法且推荐**（三级回退天然支持）：

```
基础态（成长中）   assets/plants/{speciesId}_{stage}.png            例：species_sunflower_adult.png
特殊态（枯萎/枯死/盛开） assets/plants/{speciesId}_{stage}_{status}.png  例：species_sunflower_adult_wilting.png
```

- `species_sunflower_seed_dead.png` 已存在但**实际不可达**（见美术清单 §3.2：普通植物阶段短，死亡时刻不早于 seed→sprout 推进）→ 留着无害。**普通两物种（向日葵/雏菊）不需要出 `seed_dead`；但精品仙人掌（480h/阶段）需要**（枯萎 3 天 + 死亡 7 天发生在种子期以内）。详见 `docs/美术资源清单_花园植物.md` §3.2。
- **新图入库后必须跑** `tools/normalize_plant_art.py` 归一化到 1200×2000，否则会因透明留白导致「显示过小」。

---

## 5. 🧭 下一步建议顺序

> ⚠️ 方向/优先级属**建议**，待玄参拍板；采纳后才落档。完整理由见 `docs/交接总结_下一阶段.md` §6。

```
✅ 已完成：装机复验（模拟器）→ commit + push → P0 缺口清零（2026-09-25 收口）
✅ 本轮（2026-09-25 晚 ~ 2026-09-26 累计，本日提交推送）：上架材料文档 + 成长动画方案文档 + 浇水/施肥代码动效 + 成长/商店页风格改造 + 家长端奖励/成长奶油卡美化 + 400周池简化 + 分类功能（真DB字段+固定枚举）+ 成长页卡片两段式修复 + 商店卡图标放大+限次收行内（均 463 测试绿）

① 小米 14 Pro **真机复验**：G17–G21 五轮修复 + 施肥调参 + 本轮动效/风格  ⬅ 当前最该做（玄参自排时间，AI 只构建+安装、不自查 UI）
    · 本轮改动需在真机看：浇水/施肥动效触发位置、奶油暖底风格、新增进度环头部；2026-09-26 玄参预览后**成长/商店卡片仍不满意**，提交后继续按反馈迭代
② 上架材料收尾：商店截图（玄参真机截，清单已出）+ 隐私政策**公网托管 URL / 开发者资质**占位补全
   期间美术并行产出：通用种子 2 + 雏菊 7 + 仙人掌 8 = **17 张**，出图即接（链路已贯通）
③ 养护动效二期：**开花动画 + 成长过渡序列帧**（种子→幼苗→成株），制作指南已出（`docs/植物成长动画方案_种子到成株_v1.md`），图做好后接 `care_effect_overlay` 预留的 `frames` 接口
④ 风格铺开（可选）：今日/我的 页同风格化，待玄参对成长/商店页验收满意后决定（**2026-09-26 玄参预览后卡片仍不满意，本节暂缓**）
```

> ⚠️ 上述①之后的**排序与取舍属建议**，待玄参拍板；采纳后才落档。完整理由见 `docs/交接总结_下一阶段.md` §6。

**分线原则**：「不需要决策的」连续推进不打断；「需要拍板的」攒成一批一次性拍。

---

## 6. ❓ 待玄参决策的点（攒批拍板）

| # | 决策点 | 选项 | 影响面 |
|---|---|---|---|
| A | ~~包名怎么定~~ | ✅ **已定**：`com.sunflowertime.app`（已提交 `9e6da42`） | — |
| B | ~~是否提供数据迁移~~ | ✅ **已定**：不做迁移（开发期可接受） | — |
| C | ~~花盆出图方式~~ | ✅ **已定**：花盆**走上 PNG 美术** + **植物图自带盆、代码不叠**（宪法 C13）。`pot.png` 已交付并接入 | — |
| D | **下一步走哪条路** | 路径 A（先上架闭环）/ B（先补功能缺口）/ C（先做美术） | 见 §5 与 `交接总结_下一阶段.md` §6。**现状：B 已于 2026-09-25 清零，A 只剩上架材料** |
| **E** | **养护动效用什么技术路线** | ✅ **已定：先代码后序列帧（混用）**。浇水/施肥先上纯代码动效（已落地）；开花/成长过渡待序列帧，图做好接 `care_effect_overlay` 预留的 `frames` 接口。 | 决定**出图量**与**工期**。①②可混用：先上代码动效拿反馈感，美术序列帧后补替换 |
| **G** | **`ACCESS_NOTIFICATION_POLICY` 权限是否实装** | 保留声明（专注期静音/勿扰）/ 提审前移除声明 | 许清楚核 manifest 发现该权限已声明但「专注期静音」功能疑似未接线；若未实装建议提审前移除简化隐私叙述。待工程师确认接线状态 |
| **F** | **家长赠予日上限 20 是否放宽** | 维持 20 / 放宽 | 玄参曾想赠 30 被拦（G19）；PRD §4.5 口径 20/日 · 150/月，**未擅改** |

---

## 7. 怎么用这份表（新会话开场）

**必读 4 个（恢复完整上下文）**

1. `docs/项目进度跟踪表.md` —— **本文件**，进度事实源
2. `docs/口径裁定表_v1.md` —— 项目宪法，**最高优先级**（现含 C1–C13），数值/口径冲突以它为准
3. `.workbuddy/memory/MEMORY.md` —— 长期项目记忆（协作纪律、工程硬规则、环境）
4. `docs/bug记录表.md` —— 缺陷与已拍板结论台账

**按任务再读**

| 要做什么 | 读什么 |
|---|---|
| 经济 / 数值 | `docs/产品开发文档_M2M3M4.md` + `lib/core/constants/prd_params.dart` |
| 架构 / 服务 | `docs/软件设计文档_M3M4.md` + `docs/架构设计_SunFocus_MVP.md` |
| 花园美术 | `docs/美术资源清单_花园植物.md` + **宪法 C13** + `lib/presentation/child/widgets/plant_artwork.dart` |
| 上架 / 打包 | `android/app/build.gradle.kts` + `docs/交接总结_下一阶段.md` §2 |

**机器约束（不记住会踩坑）**

- Flutter：`/Users/wangjunchao/development/flutter/bin/flutter`
- 真机：小米 14 Pro，`adb -s f05bbc46`，包名 **`com.sunflowertime.app`**
- 跑测试**必须先摘四个代理变量**（代理会拦 flutter_tester 的 WebSocket）：
  `env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy flutter test --no-pub`
- **没有 Apple 真机**（只有 iOS 模拟器，无麦克风）；需麦克风/真机的功能只能在 Android 真机测
- **同一文件的多个编辑绝不能并行下发**（会静默覆盖，已两次造成事故）
- **动作前先征得同意**；**提交必须经玄参明确允许**；AI 只构建+安装，**不截图、不自查 UI**
- 新增植物美术图后**必须跑** `tools/normalize_plant_art.py`（1200×2000 统一画布）

---

## 附：本轮（2026-09-23 大批次）新增/改动文件清单

**新增（21 项）**

| 文件 | 内容 |
|---|---|
| `lib/presentation/child/widgets/garden_background_layout.dart` | 背景覆盖映射 + 木牌热区 + 「锁 2 行」视口高纯函数（唯一真源） |
| `lib/presentation/child/widgets/garden_sign_hotspot.dart` | 木牌热区（呼吸光晕 + 按住缩放） |
| `lib/presentation/child/widgets/garden_help_sheet.dart` | **「玩法说明」**弹窗（数字全取自常量/state；原「花园说明」已按玄参拍板改名） |
| `lib/presentation/child/widgets/sunlight_pill.dart` | 顶部栏阳光胶囊 |
| `test/m3/garden_layout_v3_test.dart` | v3 布局测试 **25 条**（含真实 `GardenPage` 渲染用例） |
| `test/m3/migration_v7_to_v8_test.dart` | v7→v8 迁移测试（`bloomed_at`） |
| `test/m3/plant_wilt_recover_boundary_test.dart` | 枯萎恢复边界测试 |
| `tools/normalize_plant_art.py` | 美术归一化脚本（1200×2000，幂等） |
| `assets/plants/species_sunflower_*.png` | 向日葵 **10 张** |
| `assets/pots/pot.png` / `assets/garden/background.png` | 通用陶盆 / 草地背景 |
| `assets/_originals/` | 原图备份（**不入包**） |

**改动（31 项，主要）**

| 文件 | 内容 |
|---|---|
| `lib/core/constants/prd_params.dart` | 枯萎 3 天 / 恢复参数 / 删救回价 / 花期 3 天 / 花谢回落 0.5 |
| `lib/domain/services/plant_growth_service.dart` | 删 `revive()`、`_maybeRecover`、`_applyBloomCycle` |
| `lib/domain/entities/plant.dart` | `bloomedAt` 字段 |
| `lib/data/local/database/{tables,app_database,daos}.dart` | `bloomed_at` 列 + schemaVersion 8 + `countByRefTypeAndRefIdSince` |
| `lib/domain/repositories/sunlight_repository.dart` + 2 个实现 | 账本按时间计数 |
| `lib/presentation/child/pages/garden_page.dart` | 删底部白块、锁 2 行 + 区域滚动、木牌接线 |
| `lib/presentation/child/widgets/garden_pot.dart` | 只渲染植物图（不叠盆）、加号对齐盆心、文案胶囊底 |
| `lib/presentation/child/widgets/plant_artwork.dart` | 去圆形裁切、`growthScale=0` |
| `lib/presentation/child/pages/child_shell_page.dart` | 隐藏 tab 包 `TickerMode`（后台停帧） |
| `pubspec.yaml` | 新增 `assets/pots/`、`assets/garden/` |
| `docs/*`（7 份） | 宪法 C12/C13 + 本文档 + 美术清单 + 设计/产品文档 + bug 台账 + 交接总结 |
