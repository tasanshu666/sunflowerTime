# 花园植物 · 静态美术资源清单（v2）

> 用途：交给美术（或玄参自绘）的**出图清单**。按本清单命名丢进 `assets/plants/`，**跑一次归一化脚本即生效**。
> 依据：2026-09-23 21:50 对代码与**已交付资源**的实地盘点（非文档转述）。所有数字均带 `文件:行号` 出处。
> 范围：**只做静态图片**。动态效果（风摆、呼吸、生长补间）留到后期升级，本期不做。
> **现行口径以 `docs/口径裁定表_v1.md` C13 为准**（画布规格 / 花盆归属 / 网格布局）。

---

## 0. v1 → v2 变更摘要（**出图前必读**）

| # | v1 写的 | v2 实际（以此为准） | 依据 |
|---|---|---|---|
| 1 | §0「链路是坏的，出图不生效」 | ✅ **已修**（`2725f25`）：改用 `AssetManifest.loadFromAssetBundle` + `listAssets()`，并已实机验证「换图 → 界面真的变了」 | 提交 `2725f25` |
| 2 | §5「512×512 / 正方形 / **圆形裁切** / 主体限内切圆 86%」 | ❌ **规格已全部作废** → 新规格 **1200×2000 竖版画布**，**无圆形裁切**，详见 §5 | 宪法 **C13** |
| 3 | §6-1「花盆是自绘 `_PotPainter`，不出图」 | ❌ **已改为 PNG 美术**：`assets/pots/pot.png`（已交付并接入）；且**植物图自带陶盆，代码不再叠盆** | 宪法 C13 |
| 4 | §6-4「草地背景是自绘渐变」 | ✅ 已改为 `assets/garden/background.png`（已交付并接入，铺满 body） | 宪法 C13 |
| 5 | §3「`seed_dead` 三物种**全不可达**」 | ⚠️ **部分修正**：普通植物仍不可达，但**精品植物（仙人掌 480h/阶段）的 `seed_dead` 现在可达** → 需要出图，详见 §3 | 本轮重新推导 |

> **本轮最重要的两条**：**① 画布规格从 512×512 换成 1200×2000**；**② 仙人掌需要多出 1 张 `seed_dead`**。

---

## 1. 命名契约（文件名即接口）

**三级回退**，从最精确往回找，全部找不到才用内置自绘占位（`plant_artwork.dart` 的 `PlantArtCandidates.forPlant`）：

```
① assets/plants/{speciesId}_{stage}_{status}.png   ← 最精确（特殊态用）
② assets/plants/{speciesId}_{stage}.png            ← 该阶段通用（基础态用）
③ assets/plants/{speciesId}.png                    ← 该物种通用（不分阶段）
④ 都没有 → 内置自绘简笔（PlantPlaceholderArt）
```

**玄参本轮实际采用的命名模式（合法、推荐沿用）**：

```
基础态（成长中）      species_sunflower_seed.png / _sprout.png / _adult.png      ← 用二级
特殊态（萎/死/盛开）  species_sunflower_adult_wilting.png / _dead.png / _bloomed.png ← 用一级
```

- **只支持 PNG**（候选文件名在代码里硬编码为 `.png`）
- 目录已注册：`pubspec.yaml`（`- assets/plants/`）；`assets/pots/`、`assets/garden/` 本轮新增
- 解析结果按「物种_阶段_状态」**进程内缓存一次**，改图需重装/重启
- 坏图不崩：解码失败自动回退占位

**变量取值**（必须是这几个字符串，大小写敏感）：

| 变量 | 取值 | 出处 |
|---|---|---|
| `{speciesId}` | `species_sunflower` / `species_daisy` / `species_cactus` | `lib/data/local/plant_seed.dart:20-48` |
| `{stage}` | `seed` / `sprout` / `adult` | `lib/domain/entities/enums.dart` |
| `{status}` | `growing` / `bloomed` / `wilting` / `dead` | `lib/domain/entities/enums.dart` |

> ⚠️ `{stage}` / `{status}` 用的是**枚举名**（`.name`），不是中文、不是 index 数字。枚举顺序也不能调（数据库存的是 index）。

---

## 2. 一共几种植物：**3 种**（不是 PRD 写的 8 种）

| # | `speciesId` | 中文名 | 稀有度 | 高年段价 | 低年段价 | 阶段时长 | 自动成长 |
|---|---|---|---|---|---|---|---|
| 0 | `species_sunflower` | 向日葵 | common | **0**（初始免费） | 0 | 240h（10 天/阶段） | 10%/天 |
| 1 | `species_daisy` | 小雏菊 | common | 180 | 72 | 240h（10 天/阶段） | 10%/天 |
| 2 | `species_cactus` | 仙人掌 | **legendary（优良）** | 420 | 168 | **480h（20 天/阶段）** | **5%/天** |

出处：`lib/data/local/plant_seed.dart:20-48`（硬编码常量表，**不落库**）；时长常量 `kPlantGrowthHoursPerStageDefault = 240.0` / `kPlantGrowthHoursPerStagePremium = 480.0`。

> ⚠️ **「精品植物」= 仙人掌**（`Rarity.legendary`），它的阶段时长是普通植物的 **2 倍**、自动成长只有 **一半**。这条差异直接决定了 §3 的 `seed_dead` 可达性结论，**不要按 PRD §4.6 的「首发 8 种」出图**。

---

## 3. 阶段 × 状态：可达性矩阵（**已按枯萎 3 天重新推导**）

**3 阶段**：`seed` 种子 → `sprout` 幼苗 → `adult` 成株
**4 状态**：`growing` 成长中 → `bloomed` 已开花 → `wilting` 枯萎中 → `dead` 已死亡

### 3.1 可达矩阵

| 阶段 | growing | bloomed | wilting | dead |
|---|---|---|---|---|
| **seed** 种子 | ✅ | ❌ 不可达 | ✅ | ⚠️ **普通不可达 / 精品可达**（见 §3.2） |
| **sprout** 幼苗 | ✅ | ❌ 不可达 | ✅ | ✅ |
| **adult** 成株 | ✅ | ✅ | ✅ | ✅ |

| 物种类型 | 可达槽位 | 合计 |
|---|---|---|
| 普通（向日葵、小雏菊） | 9 / 物种 | 18 |
| 精品（仙人掌） | **10 / 物种**（含 `seed_dead`） | 10 |
| **总计** | | **28 张** |

### 3.2 `seed_dead` 可达性推导（**本轮根据枯萎 3 天重新算过**）

**关键机制**：**枯萎期间植物仍在继续长大**。tick 顺序是 `_advanceGrowth`（推进阶段）→ `_applyBloomCycle` → `_applyWiltAndDeath`（判枯萎/死亡）（`plant_growth_service.dart:248-250`），而 `_advanceGrowth` **只对 `dead` 短路、不拦 `wilting`**。

- 枯萎触发：`now - lastWaterAt ≥ 3 天` → `wilting`（`:376-382`）
- 死亡触发：`now - wiltedAt ≥ 7 天` → `dead`（`:384-390`）

**① 普通植物（240h/阶段，自动成长 10%/天）—— 不可达**

| 天 | 发生了什么 | 结果 |
|---|---|---|
| 0 | 种下 | `seed` / `growing`，进度 0 |
| **3** | 距上次浇水满 3 天 | `wilting`，进度 30%，**stage 仍 seed** |
| **10** | ① 进度 = 10 天 × 10% = **100%** → 推进 `sprout`；② 同时枯萎已满 7 天 → 死亡 | 同一 tick 内**先推进、后判死** → `dead`，但 **stage 已是 `sprout`** |

→ 死亡时刻（第 10 天）**不早于**种子→幼苗的推进时刻，所以**普通植物永远见不到 `seed_dead`**。（临界同日，实现顺序决定它落在 `sprout`。）

**② 精品植物 · 仙人掌（480h/阶段，自动成长 5%/天）—— ✅ 可达**

| 天 | 发生了什么 | 结果 |
|---|---|---|
| 0 | 种下 | `seed` / `growing` |
| **3** | 距上次浇水满 3 天 | `wilting`，进度 **15%**，stage 仍 seed |
| **10** | 枯萎满 7 天 → 死亡。此时进度才 **50%**，**离 100% 还差一半** | `dead`，**stage 仍是 `seed`** ← **这就是 `seed_dead`** |

→ 因为仙人掌一个阶段要 20 天（普通只要 10 天），**死亡来得比阶段推进早**，所以**「种子阶段的死亡」在仙人掌上真实可达**。

> **结论**：
> - `species_sunflower_seed_dead` / `species_daisy_seed_dead` → **不要出**（出了也看不到）
> - `species_cactus_seed_dead` → **要出**（否则仙人掌会有 1 个状态落到自绘占位，与其它图风格不一致）
> - 玄参已出的 `species_sunflower_seed_dead.png` **留着无害**（只是不可达，不影响任何流程）

### 3.3 另一个不可达：`bloomed` 只在 `adult`

开花的唯一赋值点是 `stage == adult && 进度 >= 1.0`（`plant_growth_service.dart` 的 `_advanceGrowth`）。种子/幼苗阶段永远不会是开花态。

### 3.4 顺带说明「美术空白」是否还需要补

v1 清单提过 3 处占位重复（`seed_wilting` 同 `seed_growing`、`adult_growing` 同 `adult_bloomed`、仙人掌 `adult` 萎/死合并）—— **这些是「内置自绘占位」的问题，只要美术图到位就自动消失**。向日葵本轮已用正式图覆盖，**雏菊与仙人掌仍需出图**才能补上。

---

## 4. 出图进度与清单

### 4.1 向日葵 `species_sunflower_*` —— ✅ **10 张已交付并接入**

| # | 实际文件名 | 槽位 | 状态 |
|---|---|---|---|
| 1 | `species_sunflower_seed.png` | seed / growing | ✅ |
| 2 | `species_sunflower_seed_wilting.png` | seed / wilting | ✅ |
| 3 | `species_sunflower_sprout.png` | sprout / growing | ✅ |
| 4 | `species_sunflower_sprout_wilting.png` | sprout / wilting | ✅ |
| 5 | `species_sunflower_sprout_dead.png` | sprout / dead | ✅ |
| 6 | `species_sunflower_adult.png` | adult / growing | ✅ |
| 7 | `species_sunflower_adult_bloomed.png` | adult / bloomed | ✅ |
| 8 | `species_sunflower_adult_wilting.png` | adult / wilting | ✅ |
| 9 | `species_sunflower_adult_dead.png` | adult / dead | ✅ |
| 10 | `species_sunflower_seed_dead.png` | seed / dead | ✅（不可达，留着无害） |

### 4.2 小雏菊 `species_daisy_*` —— ⬜ **0 / 9**

命名与上表逐一对应（把 `sunflower` 换成 `daisy`），**基础态用二级名、特殊态用一级名**：

```
species_daisy_seed.png              species_daisy_sprout.png           species_daisy_adult.png
species_daisy_seed_wilting.png      species_daisy_sprout_wilting.png   species_daisy_adult_bloomed.png
                                    species_daisy_sprout_dead.png      species_daisy_adult_wilting.png
                                                                       species_daisy_adult_dead.png
```

- **不要出** `species_daisy_seed_dead.png`（不可达，见 §3.2）
- 建议做出与向日葵的**可识别差异**（白色细长花瓣 + 黄色花心），否则用户分不清是两种植物

### 4.3 仙人掌 `species_cactus_*` —— ⬜ **0 / 10**

| # | 文件名 | 画面语义 | 备注 |
|---|---|---|---|
| 1 | `species_cactus_seed.png` | 埋在土里的健康种子 | — |
| 2 | `species_cactus_seed_wilting.png` | 种子发蔫（土面干裂） | — |
| 3 | **`species_cactus_seed_dead.png`** | **枯死的种子（灰褐、土面干硬）** | ⚠️ **本轮新增需求**，见 §3.2；普通植物不需要这张 |
| 4 | `species_cactus_sprout.png` | 小仙人掌：**无叶片**，光杆 | 形态与另两种不同 |
| 5 | `species_cactus_sprout_wilting.png` | 蔫掉的小柱 | 无叶 |
| 6 | `species_cactus_sprout_dead.png` | 枯死小柱 | 无叶 |
| 7 | `species_cactus_adult.png` | 成株未开：绿柱 + 两侧小臂 | — |
| 8 | `species_cactus_adult_bloomed.png` | **顶部开粉花** | 全 App 最有辨识度的一刻，建议优先做 |
| 9 | `species_cactus_adult_wilting.png` | 蔫掉：柱体发黄/发软下垂 | — |
| 10 | `species_cactus_adult_dead.png` | 枯死：灰化、皱缩 | — |

### 4.4 汇总

| 项 | 数量 |
|---|---|
| 总可达槽位 | **28**（普通 9×2 + 精品 10） |
| 已交付 | **10**（向日葵） |
| 待出 | **18**（雏菊 9 + 仙人掌 10，**含仙人掌 `seed_dead` 1 张**） |
| 明确**不要出** | 2（向日葵、雏菊的 `seed_dead`） |

---

## 5. 图片规格要求（**已全部更新，出图前必读**）

> ⚠️ v1 的「512×512 / 正方形 / 圆形裁切 / 内切圆 86%」**全部作废**。现行规格见宪法 **C13**。

| 项 | 要求 | 原因 |
|---|---|---|
| 格式 | **PNG**（带透明通道） | 候选名硬编码 `.png` |
| **画布** | **1200 × 2000**（宽 : 高 = 3 : 5，竖版） | 植物 + 自带花盆的整体构图；**统一画布才能保证所有盆等大、底边齐平** |
| **花盆宽度** | **800 px**（占画布宽 66.75%） | 三张图必须完全一致，否则盆会大小不一 |
| **花盆底部** | **贴合画布底边**（`y = 2000`） | 保证 9 个格子底部严格对齐 |
| **盆心水平位置** | **`x = 600`**（画布正中） | 同上 |
| 背景 | **全透明** | 无圆形裁切，四角不会被切 |
| 圆形裁切 | **已取消** | 代码已移除 `BoxShape.circle` 与圆形底色 |
| 主体缩放 | **不随进度变化**（`growthScale = 0`） | 所有盆必须恒定大小；进度反馈交给盆下细进度条 |
| 小尺寸可辨识 | 草地格子内实际显示约 **90–110 px** 宽 | 同一张图也会用在养护卡片里 |

### 出图时的构图要点

1. **主体（含盆）占满画布下半部分**，盆底贴底、盆心居中 → 上方留白给植物生长空间。
2. **不要在画布四周留大片透明留白** —— 这正是 v1 出图的**真凶**：原图 2048×2048 但内容只占 **35%×30%**，`BoxFit.contain` 按整张画布缩放 → 花盆只显示约 29px，**改任何尺寸参数都救不回来**。
3. 各状态的**盆位置必须完全一致**（同宽、同底、同心），只有植物部分变化 —— 否则切换状态时盆会跳动。

### 归一化脚本（**每批新图入库后必跑**）

```bash
python3 tools/normalize_plant_art.py
```

- 幂等：glob 扫描 `assets/plants/*.png` 与 `assets/pots/*.png`，**新增图自动纳入**
- 自动裁掉透明留白 → 按统一规则重排为 **1200×2000**（盆宽 800、盆底贴底、盆心 x=600）
- 原图备份到 `assets/_originals/`（**不在** `pubspec.yaml` 声明内，**不会打进包**）
- 脚本产出后建议逐张核对：内容底边 = 2000、口沿宽 ≈ 800、盆心 x ≈ 600

---

## 6. 除植物之外，还需要出的资源

| # | 资源 | 状态 | 说明 |
|---|---|---|---|
| 1 | **通用陶盆** | ✅ **已交付并接入** | `assets/pots/pot.png`（1200×2000，盆宽 800、盆底贴底）—— 用于**空花盆格**；有植物的格子**不叠盆**（植物图自带） |
| 2 | **草地背景** | ✅ **已交付并接入** | `assets/garden/background.png`（**1161×2560**），`BoxFit.cover` 铺满整个 body；左下角木牌已做成「花园说明」入口 |
| 3 | **空盆 / 加盆位徽章** | ⏸ **保留矢量图标** | `Icons.add` / `Icons.add_circle_outline` 加号 + 价格文案，本期不出图 |
| 4 | **打盹屏/结算页的大向日葵** | ⏸ **本期不做** | ⚠️ 这是**第二个独立的向日葵形象**（`sunflower_canvas.dart`，12 瓣、会呼吸/睁眼/送光），与花园里的小植物**不是同一套**。涉及动画，留到动态升级期 |

**关于花盆的现状**：花盆**仍然没有等级/容量相关的视觉分级** —— 所有格子共用同一个 `pot.png`。PRD §4.6 提到的「花盆作为展示权益」**在代码里不存在**。若以后想做「扩容后换更好看的盆」，那是**新需求**，需先定规则再出图。

---

## 7. 交付与新图入库流程

1. **交付**：把 PNG 按 §1 命名放进 `assets/plants/`
2. **归一化（新增步骤，必做）**：`python3 tools/normalize_plant_art.py`
3. **生效**：无需改任何代码；重装 App 即可看到
4. **验收建议顺序**：
   - 先只放 **1 张**（如 `species_cactus_adult_bloomed.png`）→ 自动归一化 → 装机确认替换生效
   - 再补齐一个物种的整套 → 确认三个阶段与各状态都能正确切换
   - 最后补另一个物种
5. **注意**：资源清单在进程内**缓存一次**，换图后需重装/重启 App（不只是热重载）
6. **不要**把原图直接丢进 `assets/plants/` 就完事 —— 未经归一化的图会因透明留白显示过小

---

## 8. 美术不必为这两件事出图

| 机制 | 为什么不用出图 | 出处 |
|---|---|---|
| `growthFactor`（当日有效专注 ×1.3，幼苗长得更快） | 落库但**不传给绘制层**，视觉上无体现 | `plant_artwork.dart` 的展示判定只比较 `speciesId/stage/status` |
| `PlantMood`（happy / calm / thirsty） | 只影响「我的」页的 emoji 文案，**不影响绘制** | `plant_card.dart:82-91` |

> `growthScale` 曾让植物随进度收缩（v1 清单里提过「图会缩一点」）—— **现已改为 0**（宪法 C13：所有盆必须等大），出图时**不需要**再为缩放预留边距。

---

## 9. 附：文档说得比代码多的地方（**别按文档出图**）

| 项 | 文档说 | 代码实际（以此为准） | 出处 |
|---|---|---|---|
| 物种数 | PRD §4.6 首发 **8 种** | **3 种** | `plant_seed.dart:20-48` |
| 阶段数 | PRD「种子→发芽→幼苗→含苞→盛开」**4 阶段** | **3 阶段**（seed/sprout/adult） | `enums.dart` |
| 阶段时长 | PRD「每阶段 24–72 小时」 | 240h / 480h（10 / 20 天） | `prd_params.dart` |
| 种植价 | PRD「180/420/**680** 三档 + 订阅 900」 | 0 / 180 / 420（**无 680 档**） | `plant_seed.dart:20-48` |
| 向日葵稀有度 | PRD 单列「初始」档 | 归入 `Rarity.common`（普通） | `plant_seed.dart:24` |
| 花盆 | PRD 定为「展示权益」（暗示有分级） | **无任何视觉分级**（共用同一张 `pot.png`） | 宪法 C13 |
| 浇水/施肥价 | PRD「20 / 8 阳光」「50 / 20 阳光」 | **固定 5 / 10 阳光**（不分年段） | `prd_params.dart:140/143` |
| 养护限次 | PRD「每阶段最多 1 次（二选一）」 | **按天**：浇水 3 次/天、施肥 1 次/天，可同时用 | `prd_params.dart:146/149` |
| 枯萎阈值 | PRD/M3 架构「7 天未浇水」 | **3 天**（2026-09-23 玄参拍板，宪法 C12） | `prd_params.dart:165` |
| 救回方式 | PRD/M3 架构「花 20/8 阳光救回」 | **取消付费救回**，改**养护恢复**（宪法 C12） | `plant_growth_service.dart:587` |

> PRD 是需求基线文档（玄参自维护），**不回写**；此处仅作对照，**出图一律以代码与宪法 C13 为准**。

---

## 附：开花节奏（供美术把握「等待感」）

| 项 | 值 | 出处 |
|---|---|---|
| 自动成长 | 10%/天（普通）、5%/天（精品） | `kPlantAutoGrowthScale = 1.0` + 阶段时长 |
| 浇水 | +1% 进度 / 次，5 阳光，**每天最多 3 次**，间隔 ≥30 分钟 | `kPlantWaterProgressGain / Cost / MaxPerDay / IntervalMinutes` |
| 施肥 | +5% 进度 / 次，10 阳光，**每天最多 1 次** | `kPlantFertilizeProgressGain / Cost / MaxPerDay` |
| 每日满养护 | 10 + 3×1 + 5 = **18%/天** | 由上述常量推出 |
| 完全不管 | 普通 **30 天**到开花（但第 3 天就枯萎、第 10 天死亡）；精品 60 天 | `plant_seed.dart:8-12` |
| 每天满养护 | 普通 **约 17 天**、精品约 23 天 | — |
| 有效专注加成 | 当日达成 WFD（≥15 分钟且完成率 ≥90%）→ 当日成长 **×1.3** | `plant_growth_service.dart` |
| **枯萎 / 死亡** | 3 天未浇水 → 枯萎；枯萎后再 7 天 → 死亡（返还 30% 成本） | `kPlantWiltDays = 3` / `kPlantDeathDays = 7` / `kPlantDeathRefundRate = 0.30` |
| **花期循环** | 盛开保持 **3 天** → 花谢（进度回落至 **0.5**，体型保持成株）→ 养满再盛开 | `kBloomDurationDays = 3` / `kBloomWiltProgressFloor = 0.5` |

→ 美术含义：**孩子每天看到的进度变化只有 3%（浇水 3×1%）～18%**，所以同一个阶段的图片会停留很久，而**状态切换（枯萎/盛开/死亡）才是视觉上的大事件**。**每个状态的辨识度比精细度更重要** —— 孩子是「一天看一点」，不是「一眼逐个放大看」。
