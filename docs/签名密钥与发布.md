# 签名密钥与发布

> 用途：钉死「向日葵专注」Android 发布签名的真源——密钥库在哪、密码在哪、换机器怎么恢复、出包怎么验签。
> 制定：寇豆码（Engineer）｜日期 2026-09-23｜状态：生效
> ⚠️ **纪律**：本文件及仓库内任何文档**都不得出现密码明文**。密码只存在 `android/key.properties`（不入库）与项目负责人的离线备份中。

---

## 1. 现状

Android release 构建**已改用自有发布密钥签名**。

- 此前（模板默认）：`android/app/build.gradle.kts` 的 `release` 沿用 `signingConfigs.getByName("debug")`，即 **debug 密钥**——属上架硬阻塞（应用商店拒绝 debug 签名包，且 debug 密钥随机器生成、无法作为长期身份）。
- 现在：`android/app/build.gradle.kts:40-49` 新增 `signingConfigs { create("release") {...} }`，`release` 块（`:52-62`，`buildTypes` 为 `:51-63`）改为读取该配置；签名参数从 `android/key.properties` 载入（`android/app/build.gradle.kts:10-17`）。
- 韧性：当 `android/key.properties` 缺失（CI / 新克隆未提供密码）时，`hasReleaseSigning=false`，release **回落 debug 签名**并打印告警，保证 `flutter run --release` 仍可本地调试；但此时产物**不可上架**。

---

## 2. 密钥库信息

| 项 | 值 |
|---|---|
| 密钥库路径 | `android/app/upload-keystore.jks`（**有意入库**，见 §5） |
| 别名（keyAlias） | `upload` |
| 密钥算法 | RSA 2048 |
| 签名算法 | SHA384withRSA |
| 有效期 | 10000 天（≈27 年） |
| 创建日期 | 2026-09-23 |
| 失效日期 | 2054-02-08 |
| 序列号 | 2167449c668031a1 |
| 证书主体（DN） | `CN=sunflowerTime, OU=Android, O=sunflowerTime, C=CN` |

**证书指纹（实测，供应用商店 / 三方 SDK 登记）：**

| 指纹 | 值 |
|---|---|
| SHA-1 | `DD:AC:94:1F:BD:64:0F:36:E4:FD:32:F6:16:F7:74:D2:DD:84:3F:DB` |
| SHA-256 | `99:49:E1:7E:3B:A5:8A:BE:20:54:27:DC:82:32:9C:01:A1:9D:07:E3:47:3F:2F:67:E5:D2:BA:DD:8E:3D:48:83` |

（SHA-256 与 release APK 内签名者证书摘要一致：`9949e17e...8e3d4883`，见 §4 验签实测。）

---

## 3. 密码存放位置

- **唯一落地处**：`android/key.properties`（已被 `android/.gitignore:11` 忽略，**绝不入库**）。
- **离线备份**：由项目负责人保存在离线位置（钥匙串 / 邮件）。
- `android/key.properties` 的 `storeFile` 使用**相对路径** `upload-keystore.jks`（Gradle `file()` 相对 `android/app/` 模块目录解析），保证换机器、换绝对路径都能用。

> 缺失密码 = 无法恢复密钥库 = 无法再给本 App 出更新包。密码与 `.jks` 必须**分开保管**。

---

## 4. 换机器恢复步骤 / 出包 / 验签

### 4.1 换机器恢复（新克隆）

```bash
# 1) clone 拿到 .jks（有意入库）+ build.gradle.kts 的签名逻辑
git clone <repo> && cd sunflowerTime

# 2) 重建 key.properties（4 行；密码从离线备份取，勿提交）
cat > android/key.properties <<'EOF'
storePassword=<密码>
keyPassword=<密码>
keyAlias=upload
storeFile=upload-keystore.jks
EOF

# 3) 出包
/Users/wangjunchao/development/flutter/bin/flutter build appbundle --release
```

### 4.2 出包命令

| 用途 | 命令 |
|---|---|
| 本地测试包（APK） | `flutter build apk --release` → `build/app/outputs/flutter-apk/app-release.apk` |
| 上架包（AAB） | `flutter build appbundle --release` |

### 4.3 验签命令

```bash
# apksigner 需要 JRE；本机无独立 JDK，指向 Android Studio 自带 JBR
export JAVA_HOME="/Users/wangjunchao/Applications/Android Studio.app/Contents/jbr/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
/Users/wangjunchao/Library/Android/sdk/build-tools/36.0.0/apksigner \
  verify --verbose --print-certs build/app/outputs/flutter-apk/app-release.apk
```

**通过判据**：`Signer #1 certificate DN` 必须是 `CN=sunflowerTime, ...`，**绝不能**是 `CN=Android Debug`。

**实测输出（2026-09-23）：**

```
Verifies
Verified using v2 scheme (APK Signature Scheme v2): true
Number of signers: 1
Signer #1 certificate DN: CN=sunflowerTime, OU=Android, O=sunflowerTime, C=CN
Signer #1 certificate SHA-256 digest: 9949e17e3ba58abe205427dc82329c01a19d07e3473f2f67e5d2badd8e3d4883
Signer #1 certificate SHA-1 digest: ddac941fbd640f36e4fd32f616f774d2dd843fdb
Signer #1 key algorithm: RSA
Signer #1 key size (bits): 2048
```

---

## 5. ⚠️ 风险与纪律

1. **`.jks` 是有意入库的**（方案 A：私有仓库 = 备份），依赖 `android/.gitignore:17` 的取反规则 `!/app/upload-keystore.jks`。
   - **若仓库将来要转公开，必须先更换密钥**，或先把 `.jks` 从 Git 历史里彻底清除（否则等同于公开了签名身份）。
   - 密码不入库 → 密钥库即使泄露也**打不开**；密码与密钥库分开保管 → 一方丢失仍可恢复。
2. **切换签名会导致 `adb install -r` 失败**：debug 签名与 release 签名不一致 → `INSTALL_FAILED_UPDATE_INCOMPATIBLE`。真机首次试装 release 包前**必须先卸载**旧包，而**卸载会清空 App 本地数据**（植物 / 阳光 / 专注记录）。请务必知情后再操作。
3. **密钥库不可重建**：文件一旦生成，**不要删掉重建**。重建 = 换签名 = 老用户无法升级。
4. **release 包的 R8 / 资源压缩现状（实测，重要）**：本项目 `android/app/build.gradle.kts` 虽**未显式设置** `isMinifyEnabled` / `isShrinkResources`（全仓无此二项），但在本工程 AGP 9.0.1 下 release **默认开启 R8 代码压缩与混淆**：
   - 证据一：`./gradlew :app:assembleRelease --dry-run` 的任务图含 `:app:minifyReleaseWithR8`；
   - 证据二：构建产物 `build/app/outputs/mapping/release/mapping.txt`（`# compiler: R8`，`compiler_version: 9.0.32`）中类/方法**被重命名**（如 `com.sunflowertime.app.MainActivity...-> O`、`IsEnabledMessage -> a`）。
   - **资源压缩（shrinkResources）仍为关闭**（任务图无 shrink 任务）。
   - **含义**：release 与 debug 的**行为可能有差异**——R8 只裁剪 / 混淆 **Java/Kotlin 宿主层与插件类**（Dart 侧 AOT 代码 `libapp.so` **不受 R8 影响**）。差异集中在**反射 / JNI / MethodChannel** 路径。
   - ⚠️ **`flutter build apk --no-shrink` 不是降险手段**：它是 `flutter_tools` 的**隐藏空转开关**——`flutter_tools/lib/src/runner/flutter_command.dart:983-991` 定义 `shrink` 标志（`hide: !verboseHelp` 隐藏），help 原文为 *"This flag has no effect. Code shrinking is always enabled in release builds."*。**别指望它**。
   - **真正的降险手段**：在 `android/app/build.gradle.kts` 的 `buildTypes.release` 加 `isMinifyEnabled = false`（必要时连同 `isShrinkResources = false`），或补 R8 / ProGuard keep 规则。
   - **行动建议**：首次真机试装 release 包时，重点冒烟**反射 / JNI / MethodChannel** 三类路径。
   - `mapping.txt` 用于 crash 堆栈还原（`build/` 已被 `.gitignore` 忽略，需另存归档）。
