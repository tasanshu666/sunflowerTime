# 《向日葵专注 · SunFocus》MVP 开发计划 v1.0

> 文档角色：开发总纲领（汇编自架构设计 + 验证计划两份子文档）
> 日期：2026-09-12
> 作者：交付总监 齐活林（主理人），汇编 高见远（架构师）/ 许清楚（产品经理）产出
> 需求来源：《向日葵专注_PRD_v2.0》（2026-09-10 终版，下称 PRD）
> 配套子文档：
> - `docs/架构设计_SunFocus_MVP.md`（架构 / 数据模型 / 任务分解 / 排期）
> - `docs/验证计划_SunFocus_G0G2.md`（G0 合规 / G1 形态 / G2 核心循环 / DoD）
> - `docs/sequence-diagram.mermaid`、`docs/class-diagram.mermaid`（工程师直接引用）
>
> ---
>
> ⚠️ **经济参数已更新（2026-09-23），本文为历史规划稿**：本文多处（§防孪生纪律 / M2 定义 / 任务表 T10 / §里程碑）仍写着旧的「**软顶三段** 0–60 全额 / 60–90 计 50% / 90–110 计 20% / 硬顶 79」，**该口径已废止**。
> 现行口径见 `docs/口径裁定表_v1.md` **C11**：专注 **1 分钟 = 1 阳光**（不再打薄）+ 年段日上限 **60 / 90 / 120**；成长奖励**独立**日上限 **79**；额度**三道拦**（选时长页灰档位 → 开始前截断 → **结算硬截断**）。
> 另：月度池在后续修订中已改为**周池**（默认 400 / 160，可调 50–500）。
> **本文与上两者冲突处，一律以 C11 与 `lib/core/constants/prd_params.dart` 为准。**

---

## TL;DR

一份可执行的 MVP 开发计划：**纯本地单机的 Flutter App（先 Android）**，核心闭环是「横屏打盹屏专注 → 产阳光 → 商店兑换 → 家长同机核销」。主线开发约 **18 周（兼职 20h/周）**，随后跑 **G2 十周核心循环验证**（判读在 T+22 周）。MVP 全程**不接账号/云同步**（Plan B 形态），但代码层预留接入点。文档所有数值与口径均直引 PRD，未重新发明参数。

---

## 1. 已锁定的设计前提（不可推翻）

| # | 决策 | 对计划的影响 |
|---|---|---|
| ① | **Plan B 纯本地单机**：无账号 / 无家庭组 / 无云同步；家长端用**本地 PIN 锁**进入、同机核销 | 砍掉 G0 网络侧绝大多数义务；但代码层须预留账号 + 云同步接入点（Repository 抽象） |
| ② | **Flutter，MVP 先 Android** | iOS / 平板后续复用同一代码库 |
| ③ | 投入 **兼职 20h/周** | 所有工时为单人兼职保守口径 |
| ④ | 本次交付**开发计划文档** | 下一步才是搭工程骨架 + 实现 |

> 产品红线（贯穿全程，来自 PRD §1.4）：是工具不是游戏（禁用「关卡/对战/玩家」等词）；隐私最小必要；低打扰、无声为主有声为辅；损失可挽回。

---

## 2. 总体架构（概要，详述见子文档）

**分层**：表现层（孩子端竖屏 + 家长端 PIN 页）/ 领域层（实体 + Repository 接口 + 领域服务）/ 数据层（Drift+SQLCipher 本地库 + 安全区 PIN + Remote 占位）。

**为账号+云同步留口的方式**：领域层只依赖接口；MVP 用 `Local*Repository` 实装；G2+ 时新增 `Synced*Repository` 或在 DI 处替换实现，领域服务零改动。`account_service` 留桩（Plan B 抛 `Unsupported`），数据模型已含 `childId` 字段（单孩子固定值），避免 V2 多档案重构。

**技术选型（面向单人维护 + AI 可读，不炫技）**：

| 维度 | 选型 |
|---|---|
| 状态管理 | Riverpod 2.x |
| 本地结构化存储 | Drift(SQLite) + SQLCipher（加密满足 §10.4 C13） |
| 设置 KV | shared_preferences |
| PIN | flutter_secure_storage（哈希 + salt 存安全区） |
| 动画 | Flutter 原生 CustomPainter（打盹屏轻量绘制，省美术管线） |
| 音频 | just_audio（主，预录台词）+ flutter_tts（备，快速迭代） |
| 常亮 / 方向 | wakelock_plus + native_device_orientation |
| 路由 / 不可变 / 时间 | go_router / freezed / intl |

**防孪生纪律（来自 PRD §5）**：所有可调数值单点定义在 `core/constants/prd_params.dart` 并逐条注释对应 PRD 节号；**夜间边界是全篇唯一收口值**（锁定页/防沉迷/可用时段统一读 `Settings.nightBoundary`，禁止出现第二个硬编码 21:00）；**产出侧阳光一律 1/min、不乘 K**；阳光流水 append-only，软顶/月池/排队三套规则可追溯对账。

> 完整分层架构图、类图、三张时序图见配套 mermaid 文件。

---

## 3. 里程碑与排期（20h/周）

### 3.1 里程碑总表

| 里程碑 | 范围（对应 PRD §8.2 裁剪） | 关键交付 | 工期 | 累计 | 验证窗口 |
|---|---|---|---|---|---|
| **M0 骨架** | 工程/导航/本地存储/合规本地化 | Flutter 工程、竖横屏+家长端导航、加密本地库、PIN 锁框架、首次启动同意流 | 2.5 周 | W2.5 | G0 并行（W1–2） |
| **M1 专注闭环** | 保留 §8.2 项 1/2/4 | 打盹屏、四档反馈、屏幕交互检测、阳光产出、结算动画、WFD 本地计算 | 4 周 | W6.5 | **G1 形态**（W4–6，10 户影子测试） |
| **M2 经济与商店核销**（承重墙） | 保留 §8.2 项 8（最高优先） | 软顶/分龄 K/奖励表/兑换队列/家长核销/免确认双条件/月度池/冷却期 | 4.5 周 | W11 | **G2 主门起跑**（W11 起 10 周） |
| **M3 花园/任务/家长端补齐** | 保留 §8.2 项 5/6(推迟)/9 | 植物养成(3种/3阶段)、任务打卡、家长端 4 页、删除入口、纪念册埋点 | 4 周 | W15 | 并行 G2 跑数 |
| **M4 打磨与上架** | 保留 §8.2 项 10（简化） | 夜间边界/防沉迷、声量账边界、Android 上架材料、性能/崩溃、文案终审 | 3 周 | W18 | G0 收口 + G2 跑数 |

- **主线构建合计 ≈ 18 周 / 336h**（含并行节省）；产品经理保守口径为 **19 周**（含集成/调试缓冲）。
- **G2 判读**：M2 完成（≈W11）起跑，10 周 → **T+22 周**给出 Go/No-Go。若二次不过改一次再跑 4 周，则 T+26 起。
- **G3/G4**（经济留存、商业化）在构建期后按 §8.1 推进，**不在本 MVP 构建排期内**。

### 3.2 开发期与验证期并行逻辑（不打架）

| 并行关系 | 为什么互不阻塞 |
|---|---|
| G0（W1–2）‖ M0 | G0 是文案/合规模板，零代码依赖，与工程骨架并行 |
| G1（W4–6）‖ M1 | G1 是形态假设验证，用可降级原型 + 影子测试即可，不等 M1 全完成 |
| G2（W11 起跑）‖ M3/M4 | G2 必须等 M2（商店核销上线）才有队列数据；其 10 周验证期覆盖 M3/M4 开发，互不阻塞 |
| 纪念册埋点（T+11）‖ G2 | §8.4 要求 T+11 周起埋原料，正好落在 G2 窗口，一次埋点同时满足 G2 与 V2 纪念册 |

---

## 4. 任务分解（核心，完整 28 项见子文档 §6）

> 估时按单人兼职 20h/周保守口径；标注「∥」表示可与他任务并行。M0 内 T02/T03/T05 并行；M1 内 T06/T07/T08/T10/T11 多可并行，仅 T09 汇流；M2/M3 同阶段大量可并行，是压缩墙钟工期的关键。

| ID | 任务 | 里程碑 | 估时(h) | 并行 |
|---|---|---|---|---|
| T01 | 项目初始化与依赖装配 | M0 | 8 | |
| T02 | 领域实体与枚举 | M0 | 10 | ∥T03/T05 |
| T03 | 本地 DB 与 DAO（Drift+SQLCipher） | M0 | 14 | ∥T02/T05 |
| T04 | Repository 接口 + 本地实现 + DI 装配 | M0 | 12 | |
| T05 | PRD 参数单点 + 工具函数 | M0 | 6 | ∥T02/T03/T04 |
| T06 | 专注引擎 FocusEngine | M1 | 14 | ∥T07/T10 |
| T07 | 在场检测 + 方向桥 | M1 | 10 | ∥T06 |
| T08 | 四档反馈呈现 + 向日葵画布 | M1 | 14 | ∥T06/T10 |
| T09 | 打盹屏专注页 + 入口页 + 结算动画 | M1 | 16 | |
| T10 | 阳光记账与软顶 | M1 | 12 | ∥T06 |
| T11 | 防沉迷服务 + 锁定页/休息页/假期 | M1 | 10 | ∥T06 |
| T12 | 奖励模板 + 月度池 + 设置项 | M2 | 12 | ∥T14 |
| T13 | 兑换服务（免确认双条件+月池+排队） | M2 | 16 | |
| T14 | 冷却服务（状态化+替代+熔断） | M2 | 10 | ∥T13 |
| T15 | 阳光商店页（兑换 UI+冷却/排队/余额） | M2 | 14 | |
| T16 | 家长端登录(PIN)+今日页(含核销卡) | M2 | 12 | ∥T17 |
| T17 | 家长核销 + 奖励配置 + 夸夸台 | M2 | 14 | |
| T18 | 语音/音频接入 + 安静环境档 | M2 | 8 | ∥T12–T17 |
| T19 | 任务打卡模块 | M3 | 14 | ∥T20/T22/T23 |
| T20 | 植物养成（3种/3阶段+软绑定） | M3 | 16 | |
| T21 | 花园页整合（植物园+图鉴+商店子页签） | M3 | 8 | ∥T22/T23/T24 |
| T22 | 家长端设置页（防沉迷/年龄档/夜间边界） | M3 | 10 | |
| T23 | 埋点体系（纪念册+指标）+ 分数化桩 | M3 | 12 | |
| T24 | 我的页/徽章/首页状态卡/完美日 | M3 | 12 | |
| T25 | 合规落地 G0（隐私规则/同意流/商店表述/降级通道/加密确认） | M4 | 12 | ∥T26/T27 |
| T26 | 动画与视觉打磨 + 低端机性能调优 | M4 | 14 | |
| T27 | 自测 + 集成联调 + 边界用例 | M4 | 16 | |
| T28 | 上架准备（商店素材/去游戏化/Android 配置） | M4 | 10 | |

---

## 5. 验证计划摘要（G0–G2，详细见子文档）

### 5.1 G0 合规重定性（Plan B 版）—— 这是上架前置阻塞项

**结论**：Plan B 无账号/无云/无传输，主张适用《儿童规定》第 28 条豁免**精神**，把网络侧全量儿童保护义务降级为**本地最小必要 + 透明告知 + 删除权**简化集；但**不主张「完全免责」**——《个保法》第 28/31 条字面不以联网为限，本机仍存昵称+年龄档+专注记录。最稳姿态＝网络侧义务降为零、本地基础义务做足、对外如实表述（不写绝对话）。

- **可免除（网络侧）**：可验证监护人身份/实名认证、专员网络备案、加密**传输**义务、泄露网络通知、跨境评估、聚合处理者特别义务、第三方委托评估。
- **必做（本地基础）**：① 本地隐私与处理说明；② 监护人本地告知同意 + 拒绝选项；③ 删除权（入口+响应+范围）；④ 最短必要期限 + 到期自清；⑤ 「专人负责」披露（本人+渠道）；⑥ 本地加密存储敏感字段；⑦ 商店页如实表述。
- **PRD §10.5 原稿必须重写**：原「家长账号存孩子的专注与成长记录」在 Plan B 无账号下失效，改为「本机本地运行、不联网、仅本机存储、不上传、可一键删除」两段文案（商店页 + 首次启动页），已起草于子文档 §1.4。

### 5.2 G1 形态验证（设备可得性 + 横屏放置意愿）

- **招募**：10 户主样本 + 2 候补，零成本私域（家长群/小红书）；筛选问卷 3 题（闲置设备/年龄档/是否愿专用化 7 天）。
- **判据**：① 设备可得性 ≥ 60%（≥6 户愿专用化）；② ≥ 7 户在 7 天内完成 ≥ 5 次横屏放置（每次 = 横屏 ≥ 5 分钟有效专注）。
- **观察表**：每日填横屏次数/最长分钟/主动拿起/家长干预/设备争抢/常亮耗电/维护耗时等（字段表见子文档 §2.3）。
- **B 计划**：首次不过 → 转「主力机 + 屏幕固定（Screen Pinning）/ 引导式访问」重测；二次不过则停。

### 5.3 G2 核心循环验证（主门 · 10 周）—— 唯一能验证「家长履约承重墙」

- **Go 阈值（§8.1 写死）**：① WFD ≥4 天/周 且 ≥50% 家庭达标；② 家长周活跃 ≥80%；③ W10 仍在用率 ≥50%。
- **20 条埋点事件表**（子文档 §3.2）：含 `focus_session_start/end`、`valid_focus_day`、`sun_earned`、`reward_redeem_request`(分母)、`reward_verified`(分子)、`reward_queue`、`monthly_pool_reset`、`parent_dau`、`praise_sent`、`garden_snapshot`、`milestone_event`、`app_retention` 等。
- **硬判据口径（防配置刷指标）**：队列充足性分龄（低 ≥1/周/户、高 ≥2/周/户）；数据质量门槛（≥6/10 周有队列事件）；额度调整后复测窗口 **4 周**。
- **WFD 定义**：一周内「当日完成 ≥1 次有效专注（≥15 分钟且完成率 ≥90%）」的天数。
- **二次不过改一次路径**：砍惩罚 → 补正向反馈 → 核销步数压到 ≤2 步；再不过则停。

---

## 6. 里程碑验收标准（DoD 摘要，逐条清单见子文档 §4）

- **M0**：Flutter 工程(Android 优先) + 竖横屏/家长端导航 + 加密本地库 + PIN 锁框架 + G0 本地隐私说明 + 首次启动同意流（含拒绝选项）+ 预留账号/同步接口。
- **M1**：打盹屏（低亮/慢频/零突事件/呼吸明暗/每 1/3 送光）+ 四档反馈（声量账 ≤3+结算1）+ 屏幕交互检测（恢复在场 10s 线性回满）+ 产出 1/min、离席停不扣、最短结算 ≥5min + 结算动画 + WFD 本地计算 + 退出路径写死。
- **M2**：阳光经济(专注 1/min、任务 12/任务、完美日 ×1.5 计入软顶) + 软顶三段(0–60/60–90/90–110/硬顶79) + 分龄 K(1.0/0.4，产出侧不乘) + 奖励表(11 项高/低+频次上限) + 兑换队列 + 家长核销 + 免确认双条件(单笔≤130/50 且 月累计≤池25%=100/40，自服务类一律0) + 月度池(400/160、超池排队下月1日放行) + 冷却期(状态化/替代/熔断)。
- **M3**：植物养成(3种/3阶段/有效专注≥15min ×1.3 否则×1.0) + 枯萎救回/死亡返还 + 任务打卡(模板+自定义/关联专注≥15min/成就累计制) + 家长端 4 页 + 删除入口(一键删+最短必要+导出成册) + 纪念册埋点。
- **M4**：夜间边界默认 21:00(可放 21:30–22:00) + 声量账边界(商店态进 ≤1/进入、7日不重复、可整体关) + 防沉迷(专注上限 60低/90中/120高、App 总 30min) + Android 上架材料 + 崩溃率<0.5%/误报率<3% + 文案终审(无绝对话/无游戏化词)。

---

## 7. 技术风险与待验证项

| 风险 | 等级 | 缓解 / 验证 |
|---|---|---|
| **P6 屏幕常亮代价**（耗电 + 厂商省电杀常亮） | 高 | G1 验证；降级：低亮定时刷新 / 系统级专注模式 / `FLAG_KEEP_SCREEN_ON`；必要时灭屏即暂停重算（**需拍板，见 §8 Q1**） |
| **国内 ROM 杀后台计时**（华为/小米/OPPO/vivo 限制后台常亮与计时） | 高 | 前台交互用持久化 `start_time` 切回重算；评估是否需前台服务维持灭屏计时（**需拍板，见 §8 Q1**） |
| 屏幕交互检测误判（支架无触摸 / 灭屏但人在） | 中 | 多信号融合（触摸+方向+亮屏）；阈值可配；误报由话术兜底 |
| 低端旧机动画性能（目标=闲置旧机） | 中 | CustomPainter 轻量绘制 + 限重绘区；M4 真机调优 |
| 加密存储落地成本 | 低 | drift + sqlcipher_flutter_libs 标准 opener |
| 月池重置/排队跨月一致性 | 中 | 启动即按 `datetime_ext` 补跑应重置次数 + `releaseQueue()` |

---

## 8. 需玄参拍板事项（合并双方，按优先级）

### 8.1 已决（2026-09-14，玄参拍板）

- **P0-1 后台计时与常亮策略 → 选定「灭屏即离席暂停」**
  - 澄清：打盹屏**设计上保持低亮常亮**（wakelock 维持，PRD §4.1.1 明确不走息屏黑屏），专注计时时屏幕本就不灭；玄参确认「灭屏了就看不见倒计时和向日葵状态」。因此正常态不存在灭屏。
  - 真正被处理的「灭屏」只有两类异常：① 孩子主动按电源键锁屏；② 系统/省电策略强制关屏或杀常亮（§7 P6 风险）。二者均按 PRD §4.3 屏幕交互检测判为「疑似离席」→ 产出暂停、亮屏恢复后在场 10 秒内线性回满。
  - **不引入前台服务维持息屏后计时**（规避国内 ROM 杀后台、省电、不打扰）。兜底：先靠 wakelock 尽力保常亮，若 G1 实测某些 ROM 保不住，再降级为低亮定时刷新 / 系统级专注模式（挂 §7 P6）。
- **P0-2 Plan B 合规定性姿态 → 选定「务实降级，无法务预算」**
  - 玄参明确不预留法务费。采用 §5.1 结论：网络侧儿童保护义务归零（无账号/无云/无传输），本地基础义务做足（§5.1 必做 7 项 + §1.4 两段文案），对外如实表述（不写绝对话）。
  - 诚实提示：单人开发者的务实选择，非法务背书；风险敞口靠「不联网 + 本地透明 + 删除权」抗辩《儿童规定》第 28 条豁免精神。

### P0 — 阻塞 M0 启动前必拍（已全部拍板，见 §8.1）

1. ~~后台计时与常亮策略~~ → **已定：灭屏即离席暂停，不引入前台服务**（详见 §8.1）。
2. ~~Plan B 合规定性姿态~~ → **已定：务实降级，不预留法务费**（详见 §8.1）。

### P1 — 影响设计，M0–M2 内应定

3. **家长端鉴权形态确认**：Plan B 下用**本地 PIN 锁**（不接短信验证码，与"无账号/无云"一致）。确认，避免代码层误接云账号。
4. **首发设备形态**：Android 手机横屏 还是 平板？PRD §9.2 开放问题 1 未决。建议 G1 仅记录、不阻塞，平板留 V2/双端。
5. **G2 二次不过止损线**：PRD 写「二次不过则停」。停产品 = 前期投入归零，是否真接受？还是转「继续迭代不强行上架」？（影响开发投入心理预期。）
6. **H4 家长失联兜底在 Plan B 重定性**：Plan B 同机核销已无「家长失联」概念，原 PRD「阳光换 App 内确定性奖励兜底」可能失效。建议改为「家长 48h 未核销则自动提示孩子替代项」。这是功能设计缺口，需定。

### P2 — 验证期事项

7. **G1 招募是否给测试家庭小礼品**（≤¥50/户，零成本私域为主）：影响样本招募速度。
8. **月度池默认值 400/160 是否直接作 G2 起点**：建议直接采用 PRD §4.8 写死值，G2 内不再调。

### 另：美术与音频资产（未阻塞，可先占位开发）

向日葵形象、3 种植物、结算动画、预录语音台词——沿用你「豆包中文提示词生成」的既有管线即可。MVP 可先用占位图/静默开发，美术在 M4 前接入。是否复用《向日葵学习大冒险》的向日葵形象、还是为新 App 单独生成一套，建议 M1 前顺手定（不阻塞）。

---

## 9. 下一步建议

1. **先拍板 §8 的 P0 两项**（常亮策略 + 合规姿态）——这两项不定，M0 的架构骨架就立不稳。
2. 拍板后，按 M0→M1 顺序进入实现，工程师（寇豆码）从 `T01 项目初始化` 起步，首版交付目标＝**M1 可跑通 + 能拿去做 G1 十户影子测试**。
3. G0 文案（§5.1 两段草稿）可现在就写，与 M0 并行，不依赖代码。
4. 美术资产走豆包管线，M1 前定形象来源。

> 本计划所有数值与口径均直引 PRD v2.0 对应章节，未重新发明参数。架构图/类图/时序图见 `docs/` 下对应文件。
